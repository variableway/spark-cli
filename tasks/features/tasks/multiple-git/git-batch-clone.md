# Git Batch Clone

1. given a github address: https://github.com/public-clis
2. clone all the repos in the address
3. clone all the repos in the address to the current directory or a given directory
4. also could filter some repos in the address by keywords
5. check if this feature is implemented, if so ,please clone the repos in the address automatically into a folder: public-clis
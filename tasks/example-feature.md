# 示例功能：为 spark 添加新命令

## 描述

为 spark CLI 添加一个新的子命令，用于展示当前所有已配置的仓库路径。

## 验收标准

- [ ] 新命令命名为 `spark repo list`
- [ ] 读取 `~/.spark.yaml` 中的 `repo-path` 配置
- [ ] 以表格形式输出所有路径及其有效性（是否存在）
- [ ] 添加对应的单元测试
- [ ] 更新 README 使用说明
